# API Keys
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=...

# Defaults
DEFAULT_PROVIDER=openai
DEFAULT_MODEL=gpt-4o
DEFAULT_IMAGE_MODEL=gemini-2.0-flash-exp
DEFAULT_IMAGE_PROVIDER=google

# Agent Behavior
DEFAULT_MAX_STEPS=50
DEFAULT_PLANNING_INTERVAL=5
DEFAULT_TYPING_DELAY=50       # Milliseconds between keystrokes (human-like typing)
DEFAULT_NETWORK_WAIT=2000     # Milliseconds to wait after network-triggering actions (navigate, back, forward, press, click)
DEFAULT_VERBOSITY=info        # Verbosity level: error, warn, info, debug, trace
